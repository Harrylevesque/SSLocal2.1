Developer notes

Make start.sh executable locally:

```bash
chmod +x start.sh
```

Run locally in macOS:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
./start.sh
```

